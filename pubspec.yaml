name: getx_internationalization
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  get:
  country_picker: ^2.0.5
  cupertino_icons: ^1.0.2
  find_dropdown: ^1.0.0
  country_list_pick: ^1.0.1+4
  country_pickers: ^2.0.0
  http: ^0.13.0
  http_utils: ^1.6.3
  flutter_svg: ^0.22.0
  im_stepper: ^0.1.3
  flutter_switch: ^0.3.1


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
  
  assets:
    - assets/images/logo.png